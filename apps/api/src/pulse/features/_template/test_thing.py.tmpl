# Rename to test_<name>.py when copying the template.
# Fixtures available from apps/api/conftest.py: `session` (DB) and `client` (HTTP).
#
# Cover at minimum:
#   - service happy path
#   - NotFoundError for a missing id
#   - full CRUD over HTTP via `client`
#   - one validation failure (422)
#   - ActivityEvent recorded on create
